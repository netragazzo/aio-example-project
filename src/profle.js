
function updateProfile ({firstname, email, favoriteColor}) {
    let userProfile = {firstname, email, favoriteColor};
    userProfile.meta = {}
}

function getProfile({email}) {

}

module.exports = { updateProfile, getProfile };
